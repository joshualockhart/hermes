LiDE is a small fileserver. To host a file, do

$ ./lide file_to_send

then to retrieve it on another host, do something like

$ nc <LiDE host> 9999 > file_to_receive

LiDE defaults to TCP/9999, but the port can be specified with -p.
